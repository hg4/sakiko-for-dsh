# ============================================================
# 行尾策略：blob 与工作树**都用 LF**，不依赖任何人的 core.autocrlf
# ============================================================
# 为什么显式写死：本机 core.autocrlf=true（系统级），而本仓库的 blob 里混着两种行尾。
# 不写死的话，同一份内容在不同机器上被签出/回写时会来回翻转，diff 里出现整文件"改动"。
# `eol=lf` 让签出与入库都固定成 LF ⇒ 工作树字节可复现、与 blob 逐字节一致。
# 本仓库**不含**任何 .bat / .cmd / .ps1 / .vbs（实测 140 个跟踪文件里 0 个），
# 所以 `eol=lf` 不会破坏"Windows 脚本必须 CRLF"那类需求。
* text=auto eol=lf

# ------------------------------------------------------------
# 二进制资产：显式声明，不参与文本规范化、不参与 diff
# ------------------------------------------------------------
# （text=auto 本来也会按内容嗅探出二进制，这里显式写死是为了不依赖启发式；
#   也免得将来有人加了 `* text` 这类规则把它们卷进行尾转换。）
*.png binary
*.mp3 binary
*.wav binary
*.moc binary

# ⚠️ .mtn（Live2D 动作定义）**也**必须显式排除在文本规范化之外 —— 它不是"行尾抖动"，是数据：
#   实测（2026-09-16）52 个 .mtn 的**数据行**行结构是 `<CR><CR><LF>`（文件头那 7 行是普通 CRLF；
#   末行以裸 CR 结尾、无 LF）。idle01.mtn 实测：普通 CRLF 段 7、`<CR><CR><LF>` 段 118、
#   裸 CR=119、裸 LF=0（CR=244 / LF=125）。也就是说每条数据后面多带一个 **CR 字符**。
#   证据链：
#     · git 一直把它判为**非文本**（`git ls-files --eol` → `i/-text w/-text` ⇒ 不参与行尾规范化），
#       `git hash-object` 加不加 --no-filters 同值 ⇒ **autocrlf 从未碰过它**，
#       工作树 == 索引 == blob 逐字节一致 ⇒ 它本来就没有"历史行尾抖动"需要归一。
#     · 若按文本归一（CRLF→LF），每行都会被删掉那个 CR 字节（17965 → 17840 字节）。
#   归一后 Live2D 读到的数据确实**完全一样**（已用仓库自带运行时真身
#   `plugin/web/vendor/live2d21.min.js` 的 `Live2DMotion.loadMotion` 实测：motions/yT/fps/
#   逐帧值全等，52/52 文件一致）——因为该解析器把 `\r` 与 `\n` **同等**只当行分隔符。
#   但本批次是「仓库卫生」，不该顺手改动 52 个预置动作文件的资产字节 ⇒ 显式声明为 binary，
#   把"这些字节是资产数据"写成策略，而不是留一条会被后来者误伤的隐式启发式。
#   ⚠️ 代价（2026-09-16 实测，取舍已明说）：`binary` 同时关掉 text diff ⇒ 将来改动作文件
#   只能看到 `Binary files differ` 与字节数变化。实测对照：main 态（未声明）把 idle01.mtn 改一个
#   字节给的是文本 diff（`1 insertion(+), 1 deletion(-)`）；本分支声明后同样改动变成
#   `Bin 17965 -> 17965 bytes`。本批次判断这个代价可接受 —— 资产由工具生成、改动仍可见字节数、
#   而"被当成文本删掉 CR 字节"要危险得多。
*.mtn binary

# 说明：仓库里 140 个跟踪文件的扩展名分布（2026-09-16 实测）是
#   .mtn(52) .json(35) .js(12) .mjs(8) .py(7) .md(7) .wav(6) .txt(3) .png(2)
#   .css(1) .html(1) .mp3(1) .moc(1) .yml(1) + 2 个无扩展名
# 除上面显式列出的二进制之外，其余扩展名（.json/.js/.mjs/.py/.md/.txt/.css/.html/.yml）
# 都是文本，由 `* text=auto eol=lf` 覆盖。
# ⚠️ `.moc`（Live2D 编译模型）本身是二进制容器，扩展名却像模型定义 —— 所以单独列出来。
